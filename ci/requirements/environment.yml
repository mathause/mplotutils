name: mplotutils-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - cartopy
  - matplotlib-base
  - numpy
  - seaborn
  - xarray
# formatting
  - black
  - ruff
# for testing
  - pytest
  - pytest-cov
